- rest:
    get:
      - path: /product
        to:
          uri: direct:product

- rest:
    get:
      - path: /cart
        to:
          uri: direct:cart

- rest:
    get:
      - path: /order
        to:
          uri: direct:order